# Copy this file to vault.yml (gitignored, never commit real values).
# SSH login password for pve-wazuh's ansible_user (needs `sshpass` installed
# on the control node for ansible_ssh_pass to work).
vault_wazuh_ssh_pass: REPLACE_ME
