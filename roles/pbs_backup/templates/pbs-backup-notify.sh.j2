#!/bin/sh
set -e
HOST=$(hostname)
curl -sf -H "Content-Type: application/json" \
  -d "{\"content\": \"⚠️ CephFS backup (pbs-backup.service) failed on ${HOST}\"}" \
  "{{ pbs_backup_discord_webhook }}"
